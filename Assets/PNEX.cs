using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PNEX : Enemy
{
    // Start is called before the first frame update
    void Start()
    {
        InitAwake();
    }

    // Update is called once per frame
    void Update()
    {
        InitStart();
    }
}
